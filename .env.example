# Copy to .env and adjust. The same defaults are in docker-compose.yml, so the
# stack runs without this file too.

POSTGRES_DB=alarm
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_PORT=5432
DB_SSLMODE=disable

API_PORT=8080
LOG_LEVEL=info
